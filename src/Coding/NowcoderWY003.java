package Coding;

import java.util.Scanner;

public class NowcoderWY003 {
    public static void main(String[] args) {
        Scanner in = new Scanner(System.in);
        while(in.hasNext()){
            int n=Integer.parseInt(in.next());
            int m=Integer.parseInt(in.next());
        }
    }
}
